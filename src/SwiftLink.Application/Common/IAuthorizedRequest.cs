namespace SwiftLink.Application.Common;

/// <summary>
/// This interface marker is aimed to detect wich command and query must go through the SubscriberAuthorizationBehavior.
/// </summary>
public interface IAuthorizedRequest
{
}
